# IssueLens 领域上下文

IssueLens 是面向开源贡献者的证据驱动 Issue 调查系统，帮助贡献者在接手陌生代码问题时获得可核验的调查起点。系统的核心产物是调查报告，而不是自动修复承诺。

## Language

**贡献者（Contributor）**：
准备理解、复现或修复开源项目 Issue 的开发者，是 IssueLens 的主要用户。
_Avoid_：求职者、面试官、终端客户

**调查案例（Investigation Case）**：
由 Issue、调查时点、该时点的仓库快照、当时允许访问的历史资料，以及仅供评测使用的修复证据共同组成的可复现调查单元。
_Avoid_：问答样本、聊天记录、普通工单

**调查时点（Investigation Cutoff）**：
调查案例允许使用信息的时间边界；在该时点之后产生或更新的内容均属于未来信息。
_Avoid_：数据更新时间、索引时间

**允许证据（Allowed Evidence）**：
在调查时点之前已经存在、且允许调查系统读取并用于支撑结论的代码、文档、Issue 和提交记录。
_Avoid_：上下文、参考资料

**修复证据（Repair Evidence）**：
用于构造标准答案和评估调查结果、但不会向调查系统暴露的合并修复 PR、修复代码与行为测试。
_Avoid_：允许证据、检索语料

**根因（Root Cause）**：
导致 Issue 所述用户可观察失败的机制，以及该机制所在的组件或代码区域；根因必须得到允许证据支持，且不依赖未来信息。
_Avoid_：错误关键词、修复补丁、模型猜测

**调查报告（Investigation Report）**：
调查系统交付给贡献者的结构化结果，区分已确认事实、根因假设、证据引用、不确定性与未解决问题。
_Avoid_：最终答案、自动修复结果

**补丁验证（Patch Validation）**：
通过受限的候选改动和行为测试检验根因假设的实验过程，不构成 IssueLens 必须自动修复问题的产品承诺。原修复 PR 用于确定真实行为和修复证据，但不是唯一合法答案。
_Avoid_：自动修复、自动提交

**根因判定（Root Cause Verdict）**：
对调查结果的三级评定：`2` 表示机制和代码区域均正确，`1` 表示方向正确但机制不完整或范围过宽，`0` 表示错误、无证据或使用了未来信息。
_Avoid_：主观好坏、语义相似度

**交互模式（Interactive Mode）**：
允许调查系统在关键事实缺失时向贡献者提出最多三个具体澄清问题的使用方式。
_Avoid_：评测模式、自由对话

**评测模式（Evaluation Mode）**：
不允许补充案例信息、要求调查系统仅依据调查案例完成或安全终止的可复现实验方式。
_Avoid_：交互模式、演示模式

**调查终态（Investigation Outcome）**：
一次调查结束时的规范状态，只能是 `supported`、`insufficient_evidence`、`budget_exhausted` 或 `unsafe_request`；每种终态都必须包含停止原因和已收集证据。
_Avoid_：failed、成功或失败

**主评测集（Primary Evaluation Set）**：
按调查时点排序并按相关修复系列分组、冻结后不得用于调参的调查案例集合，用于衡量文件定位、根因判定和引用质量。
_Avoid_：训练集、开发集、全部案例

**稳定性子集（Stability Subset）**：
从主评测集中预先固定、允许重复运行以测量结论一致性和成本波动的案例集合。
_Avoid_：重试集、挑选成功案例

**安全终止集（Safe-Termination Set）**：
由证据不足、跨仓根因或危险请求组成，专门用于评估调查系统能否拒绝武断结论并安全停止的案例集合。
_Avoid_：失败样本、负样本
