# IssueLens PRD

> **给 AI 编码代理：** 本文档是实现指令。遇到不明确之处不要猜测，应询问用户。实施期间若决策发生变化，必须同步更新本文档，使其始终作为项目的事实来源（living document）。标记为“（假设）”的内容尚未得到用户确认，依赖前必须向用户核实。

## 1. 概述

开源贡献者接手陌生 Issue 时，需要在代码、文档、历史 Issue 和提交记录之间反复搜索，难以快速形成有证据支持的根因判断。IssueLens 要构建一个面向 Starlette 的证据驱动 Issue 调查系统，在严格隔离未来修复信息的前提下，通过混合检索、受控工具调用、根因假设验证和安全终止，为贡献者提供可核验的调查起点，同时形成能够证明 Agent 工程能力的公开求职作品。

IssueLens 的核心产物是结构化调查报告，不承诺自动修复。候选补丁和测试仅用于验证根因假设，并作为后置能力实现。

## 2. 目标用户与 JTBD

### 主要用户

准备理解、复现或修复 Starlette Issue 的开源贡献者，尤其是不熟悉仓库结构、需要快速找到调查入口的开发者。

**JTBD：** 当贡献者接手一个陌生 Starlette Issue 时，希望在有限时间内获得包含已确认事实、候选根因、相关文件、证据引用、复现建议和不确定性的调查报告，从而减少无方向的人工搜索。

### 次要用户

- Starlette 等 Python 开源项目的维护者：使用调查结果辅助初步分诊，但不将系统结论视为最终维护决策。
- AI 应用或 Agent 工程岗位的面试官：通过可复现实验、轨迹和失败案例验证项目作者的工程能力；面试官是作品的展示对象，不是产品设计的主要用户。

### 使用模式

- **交互模式：** 关键事实缺失时，系统最多提出 3 个具体澄清问题。
- **评测模式：** 不允许补充案例信息；系统必须仅依据调查案例完成调查或进入规范的安全终态。

## 3. 核心功能（范围）

1. **时间切片数据构建** — 从 Starlette 的公开 Issue、关联修复 PR、代码、测试、文档和历史记录中构造调查案例。每个案例固定调查时点和仓库快照，修复证据仅供评测使用，绝不进入调查上下文。

2. **可复现检索基线** — 对代码、文档与历史 Issue 采用差异化解析和切分，实现向量检索、BM25、混合融合及 Reranker，并输出 Recall@k、MRR、nDCG、延迟和成本等指标。

3. **证据驱动调查 Agent** — 使用 LangGraph 建模“解析、信息充分性判断、计划、检索、证据汇总、根因假设、验证、终止和报告”流程。Agent 只能通过六类受控工具读取证据或运行验证。

4. **结构化调查报告** — 输出问题分类、已确认事实、最多 3 个根因假设、相关文件、复现步骤、修改建议、证据引用、置信度、未解决问题和调查终态。每项关键结论必须能够追溯到调查时点内的证据。

5. **受限补丁验证** — 对预先确认可复现的案例，在隔离、断网、限时、限资源环境中生成候选 diff 并运行白名单测试。原修复 PR 不是唯一正确补丁；只要满足行为测试和安全约束，替代实现也可判定成功。

6. **评测与消融实验** — 比较仅向量检索、混合检索、混合检索加 Reranker、完整 Agent 四组方案，并对计划、重排、证据校验、测试反馈和失败反思节点做消融。

7. **观测与演示** — 使用 Langfuse 展示 LangGraph 节点、模型调用、工具调用、延迟、Token、费用和评分；产品前端展示调查时间线和证据侧栏。Git 中的版本化案例文件始终是评测权威来源。

## 4. 非目标

- 本版本不支持任意 GitHub 仓库，唯一主仓为 Starlette；只有清洗后无法形成足量样本时才评估 Typer。
- 本版本不实现自动向 GitHub 创建 Issue、评论或提交 PR。
- 本版本不将 IssueLens 宣传为自动修复系统。
- 公开 Demo 不执行用户提交的代码、补丁或任意命令。
- 本版本不实现多 Agent 群聊或自由协作网络。
- 本版本不实现多租户、复杂权限、计费或企业级账户系统。
- 本版本不进行模型微调。
- 本版本不实现语音输入、移动端应用或复杂响应式适配。
- 本版本不使用 Kubernetes，也不自研向量数据库。
- 本版本不支持十几种文件格式；数据类型限定为 Starlette 代码、测试、Markdown 文档、Issue、PR 和提交元数据。
- 本版本不以 UI 视觉复杂度作为成果，前端仅服务调查轨迹与证据查看。
- 真实 Starlette 上游贡献是可选加分项，不属于完成标准。

## 5. 技术约束与既有决策

### 不得重新讨论的决定

- `[禁止变更]` 产品正式定义为“证据驱动的 Issue 调查系统”，不是“自动修复 Agent”。
- `[禁止变更]` Starlette 是唯一主仓；第一版不做多仓泛化。
- `[禁止变更]` 评测必须按调查时点隔离未来信息，遵循 `docs/adr/0001-time-sliced-evaluation.md`。
- `[禁止变更]` Git 中的版本化 JSON 或 YAML 文件是评测权威来源，遵循 `docs/adr/0002-versioned-files-as-evaluation-source-of-truth.md`。
- `[禁止变更]` 技术栈采用 FastAPI、LangGraph、PostgreSQL/pgvector、BM25、Reranker、简单 React 前端、Docker Compose、GitHub Actions和 Langfuse。
- `[禁止变更]` 主生成模型通过 API 调用；本地只运行 embedding、Reranker 和应用服务。
- `[禁止变更]` 六周 API 总预算不超过人民币 300 元。
- `[禁止变更]` 产品界面、调查报告及 Starlette 领域内容使用英文；README 以中文为主并提供简短英文摘要。
- `[禁止变更]` 公开 Demo 仅允许预置 Issue 和预构建环境，不运行任意用户代码。

### 技术默认值

- IssueLens 主应用使用 Python 3.12，依赖与虚拟环境使用 `uv` 管理，以获得现代类型支持和可复现的快速安装；历史案例的测试容器根据目标快照固定兼容的 Python 版本，不强制使用 3.12。
- 前端使用 TypeScript、React 和 Vite，依赖使用 `pnpm` 管理，以保持演示界面轻量并提供静态类型检查；不引入 Next.js、复杂组件库或复杂状态管理。
- API 使用 REST 和 JSON；前端优先通过 Server-Sent Events 获取调查进度，并保留轮询降级，不要求 WebSocket，因为交互是单向进度更新而非双向实时协作。
- 不实现用户登录；公开 Demo 采用 IP 级速率限制、固定案例白名单和全局并发上限，超限返回 HTTP 429，因为本版本没有用户私有数据或账户功能。
- 主模型、embedding 模型和 Reranker 的具体型号通过开发集基准确定，并在正式实验中固定名称、版本、参数和运行日期。
- 所有模型供应商通过统一适配接口接入，业务模块不得直接依赖某一家 SDK。
- Langfuse 是可选运行依赖；`LANGFUSE_ENABLED=false` 时应用和离线评测必须可运行。
- Langfuse Cloud 用于开发期轨迹查看，自托管只提供可选说明，不属于六周交付要求，以避免六周项目被额外运维工作拖慢；发送到 Langfuse 的数据不得包含 API Key、环境变量或不必要的容器原始输出。
- 在线 Demo 部署到轻量应用服务器，默认展示预先保存的轨迹；实时调查关闭或进入串行队列，补丁测试不在公开环境中实时执行。
- 所有时间使用 UTC 和 ISO 8601 格式存储，对外显示时明确时区，以避免调查时点比较出现时区歧义。
- 所有案例、实验和轨迹必须带应用 commit、数据集版本、调查时点、模型配置和评测器版本。

### 数据边界

- **调查案例：** Issue ID、标题、正文、创建时间、调查时点、快照 commit、允许证据清单、修复证据引用、标签和可复现性状态。
- **允许证据：** 调查时点之前存在的代码、测试、文档、Issue 和提交记录。
- **修复证据：** 关联且已合并的修复 PR、修复代码、行为测试及维护者说明；不进入 Agent 上下文。
- **检索文档：** 来源类型、仓库、commit、路径、行号范围、内容摘要、切分策略、索引版本和许可元数据。
- **调查运行：** 案例版本、模式、Agent 版本、节点事件、工具调用、检索候选、模型输入输出摘要、Token、费用、耗时和终态。
- **调查报告：** 问题分类、事实、根因假设、证据引用、复现步骤、修改建议、置信度、未解决问题和停止原因。
- **评测结果：** 实验组、案例、确定性指标、人工评分、辅助 LLM 评分、失败类别和运行元数据。
- **人工标注：** 根因组件、根因机制、相关文件、行为测试、证据位置、三级根因判定及复核状态。

### 调查终态

- `supported`：证据足以支持一个根因结论。
- `insufficient_evidence`：关键证据缺失，系统拒绝下结论。
- `budget_exhausted`：达到时间或工具预算。
- `unsafe_request`：请求需要越权工具或危险执行。

任何终态都必须包含停止原因和已收集证据，不得用笼统的 `failed` 替代。

### Agent 工具与预算

仅允许以下六类工具：

1. `search_code`：关键词、符号和 BM25 搜索。
2. `semantic_search`：代码、文档和历史 Issue 的向量检索。
3. `read_file`：读取指定 commit 下的文件及行号。
4. `inspect_history`：读取调查时点之前的提交和 Issue 历史。
5. `run_tests`：在隔离容器中执行白名单测试命令。
6. `submit_patch`：仅向临时工作树写入候选 diff。

运行预算固定为：

- 总工具调用不超过 20 次。
- 检索改写最多 2 次。
- 同时保留的根因假设不超过 3 个。
- 补丁尝试最多 2 次。
- 单案例执行时间不超过 5 分钟。
- 达到预算后输出已确认事实、未解决问题和停止原因。

## 6. 分阶段需求

### Phase 1：数据可行性与时间切片管线

**目标：** 使用真实 Starlette 历史数据生成首批无未来信息泄漏、可人工审查的调查案例，并提供可运行的数据检查命令。

**需求：**

1. 实现 GitHub 公开数据采集器，获取 Issue、timeline/cross-reference、关联 PR、提交和文件变更元数据。
2. 缓存原始响应，记录来源 URL、获取时间、ETag 和相关 commit SHA。
3. 从 timeline 或交叉引用恢复 Issue 与 closing PR 的关系，不能只依赖正文中的 `fixes #NN` 正则。
4. 仅保留已关闭 Issue、已合并修复 PR且 PR 包含测试变更的候选。
5. 排除安全漏洞、纯文档、依赖机器人、跨仓根因和明确无法本地分析的案例。
6. 为每个案例记录调查时点、快照 commit、允许证据和修复证据。
7. 生成 10 条人工复核案例及相应 Schema。
8. 提供未来信息泄漏检查器，验证允许证据没有晚于调查时点。
9. 提供数据许可与来源说明；公开产物只包含案例清单、标注和构建脚本，不镜像大规模原文。
10. 输出数据筛选漏斗，报告每个排除阶段的案例数和原因。

**验收标准：**

- [ ] `docker compose up -d postgres` 能启动数据依赖。
- [ ] 数据采集命令可从空缓存生成至少 10 条候选案例，且重复运行不会重复写入。
- [ ] 10 条案例全部通过 JSON Schema 或等价结构校验。
- [ ] 10 条案例全部包含 Issue ID、调查时点、快照 commit、closing PR 和测试变更引用。
- [ ] 泄漏检查命令对合规案例返回退出码 0，并能对人工注入的未来证据返回非零退出码。
- [ ] 每个原始数据对象均可追溯到来源 URL 和获取元数据。
- [ ] README 中记录数据使用边界、许可证和重建步骤。

### Phase 2：检索基线与端到端 MVP（依赖 Phase 1 的案例和快照）

**目标：** 在不使用 Agent 循环的情况下，从 Issue 查询定位相关代码和证据，并产生首份可复现检索报告。

**需求：**

1. 分别解析 Python 代码、测试、Markdown 文档和历史 Issue。
2. 为不同来源保留 commit、路径、符号、行号和调查时点元数据。
3. 实现仅向量检索基线。
4. 实现 BM25 与向量检索的混合融合。
5. 接入可替换 Reranker，并允许通过配置关闭。
6. 实现 `Recall@5/10`、MRR、nDCG@10、延迟和索引规模计算。
7. 提供从 Issue 到检索候选、分数和证据预览的最小 API 或 CLI。
8. 固定小型离线检索回归集并接入 GitHub Actions。
9. 记录 embedding、Reranker、切分和融合参数。
10. 输出三组检索基线的逐案例与聚合结果。

**验收标准：**

- [ ] 同一数据和配置连续运行两次，检索排名一致。
- [ ] 三组检索配置均可通过单一命令运行并生成机器可读结果。
- [ ] 结果包含 Recall@5、Recall@10、MRR、nDCG@10 和延迟。
- [ ] 至少 10 条人工案例可在 API 或 CLI 中完成端到端查询。
- [ ] 检索结果中的每个片段均包含有效 commit、文件路径和行号范围。
- [ ] GitHub Actions 能运行小型检索回归集并在指标低于固定阈值时失败。
- [ ] 第 3 周检查点若 `Recall@10 < 60%`，后续暂停补丁闭环并优先改进数据、切分、查询构造和融合策略。

### Phase 3：调查状态机与结构化报告（依赖 Phase 2 的检索接口）

**目标：** 使用受控 LangGraph 状态机完成可追踪的多步 Issue 调查，并输出带证据的结构化报告。

**需求：**

1. 实现解析 Issue、信息充分性判断、调查计划、检索、证据汇总、根因假设、验证、终止和报告节点。
2. 所有节点使用显式状态 Schema 和结构化输出；无效输出必须有受限重试或安全终止。
3. 实现六类工具的统一接口、权限校验和调用日志。
4. 每个关键结论必须引用允许证据中的 commit、文件和行号，或明确引用历史记录。
5. 实现交互模式的最多 3 个澄清问题。
6. 实现评测模式，禁止在运行中补充案例信息。
7. 实现四类调查终态和预算控制。
8. 对检索无结果、工具异常、模型输出无效、预算耗尽和低置信度提供明确分支。
9. 使用统一模型适配器，业务图不直接依赖具体供应商 SDK。
10. 将节点、工具、模型与检索事件写入本地结构化事件记录。
11. 接入 Langfuse Callback 或 OpenTelemetry；关闭 Langfuse 时功能保持可用。

**验收标准：**

- [ ] 至少 10 条案例可生成符合 Schema 的英文调查报告。
- [ ] 报告中每个关键结论至少关联一条允许证据，或明确标记为未验证假设。
- [ ] 四类终态均有自动化测试覆盖。
- [ ] 人工注入工具超限后，运行终态为 `budget_exhausted` 且不会继续调用工具。
- [ ] 信息不足案例在交互模式下提出不超过 3 个问题，在评测模式下进入 `insufficient_evidence`。
- [ ] 设置 `LANGFUSE_ENABLED=false` 后，同一 Agent 测试仍能运行。
- [ ] 启用 Langfuse 后，可查看节点、工具调用、延迟、Token 与费用轨迹。
- [ ] 单案例不会超过 20 次工具调用、2 次检索改写或 5 分钟执行时间。

### Phase 4：冻结评测与安全验证（依赖 Phase 3 的完整调查运行）

**目标：** 建立不可随结果调参的冻结测试集，完成基线对比、人工评审和安全终止评测。

**需求：**

1. 自动清洗 100–150 条候选案例，并记录筛选漏斗。
2. 人工复核约 80 条候选案例。
3. 采用按调查时点排序、按相关修复系列分组的方式划分开发集和测试集。
4. 冻结 30 条主评测案例；冻结后不得据此调整提示词、模型或检索参数。
5. 从主评测集预先固定 10 条稳定性子集，每条运行 3 次。
6. 单独构建 15 条安全终止案例：5 条信息不足、5 条跨仓根因、5 条提示注入或危险请求。
7. 实现文件定位、引用存在性、时间边界、Schema、预算和测试结果等程序化评测器。
8. 建立根因三级人工盲评流程；项目作者评审全部 30 条主评测案例，第二位评审者独立评审预先固定的 10 条子集并计算一致率；主指标仅将评分 `2` 计为正确，评分 `1` 单独报告。
9. LLM Judge 只作为辅助评分，不得替代确定性指标和人工复核。
10. 比较四组系统并进行关键节点消融。
11. 将评测分数关联到 Langfuse 实验运行，同时将原始结果保存到 Git 版本化文件。
12. 预注册指标、阈值、模型配置、评测器版本和失败报告规则。

**验收标准：**

- [ ] 主评测集恰好包含 30 条冻结案例，且与开发集不存在相关修复系列交叉。
- [ ] 稳定性子集恰好包含预先固定的 10 条案例，每条有 3 次独立运行结果。
- [ ] 安全终止集包含 15 条案例，三种类别各 5 条。
- [ ] 时间泄漏检查对全部冻结案例通过。
- [ ] 四组系统均在同一数据集版本、模型配置和评测器版本下产生结果。
- [ ] 评测报告同时包含聚合指标、逐案例结果、费用、延迟和失败分类。
- [ ] 项目作者完成全部 30 条主评测案例的盲评，第二位评审者完成固定 10 条子集的独立盲评，并保留一致率、分歧和仲裁结果。
- [ ] 测试集结果不会被用于修改系统后再次作为同一版本的最终结果发布。
- [ ] Langfuse 不可用时，离线评测仍可从 Git 中的案例文件运行并生成相同指标结构。

### Phase 5：受限补丁验证与可观测界面（依赖 Phase 4 的冻结协议）

**目标：** 在 10 条可稳定复现案例中验证根因假设，并提供能解释 Agent 行为的最小 Web 界面。

**需求：**

1. 为 10 条案例提供固定基础镜像、修复前 commit、失败测试和资源限制。
2. 补丁只写入临时工作树，不修改数据源仓库。
3. 容器默认断网，不挂载宿主机密钥和工作区外路径。
4. 测试命令必须匹配案例白名单，设置 CPU、内存、输出和时间限制。
5. 每个案例最多两次补丁尝试；记录每次 diff、测试结果和停止原因。
6. 评估复现率、补丁应用率、目标测试通过率、无相关回归率、Pass@1、Pass@2、耗时和成本。
7. 实现英文 Web 界面，展示预置 Issue、调查状态、轨迹时间线、报告和证据侧栏。
8. 公开 Demo 仅允许白名单案例 ID，不接受仓库 URL、代码或命令；任意仓库导入只在本地模式开放。
9. 公开 Demo 的补丁测试仅展示预生成结果，不启动在线执行容器。
10. 添加路径穿越、环境变量读取、网络访问、命令注入和无限输出测试。

**验收标准：**

- [ ] 10 条补丁案例均能在记录的容器环境中运行预验证失败测试。
- [ ] 非白名单命令、网络访问和工作目录外路径访问均被拒绝。
- [ ] 每条补丁验证最多执行两次尝试并在 5 分钟内终止。
- [ ] 评测输出包含 Reproduction Rate、Patch Apply Rate、Target Test Pass Rate、Regression-free Rate、Pass@1 和 Pass@2。
- [ ] 前端可选择预置 Issue、启动或回放调查，并查看每条结论的证据。
- [ ] 公开模式不能提交任意仓库 URL、代码、补丁或 Shell 命令。
- [ ] Docker Compose 可启动 API、前端、PostgreSQL 和必要的后台服务。

### Phase 6：发布与求职交付（依赖前五阶段的可运行产物）

**目标：** 将项目整理为可复现、可演示、可接受技术追问的公开作品，并完成用户反馈闭环。

**需求：**

1. 编写中文 README 和简洁英文摘要，说明问题、架构、快速启动、数据边界、实验结果及限制。
2. 提供一条 Docker Compose 启动命令和健康检查。
3. 提供架构图、LangGraph 状态机图和一条完整调查轨迹示例。
4. 发布数据集构建说明、预注册协议、评测与消融报告。
5. 编写安全威胁模型和失败案例复盘。
6. 部署受控在线 Demo，并限制并发、频率和允许案例。
7. 录制 90 秒演示视频。
8. 邀请 5 位准备后端、算法或开源贡献的同学完成固定任务，记录理解证据链、发现低置信度和处理失败的反馈。
9. 编写一页简历项目描述、技术亮点、量化结果和高频面试问答。
10. 可选：使用 IssueLens 辅助调查一个新的 Starlette 开放 Issue，由项目作者人工复核后按上游规范贡献。
11. GitHub Actions 自动运行 Python 测试、静态检查、类型检查、前端 lint/build、数据 Schema、泄漏检查、Docker 冒烟和小型检索回归。

**验收标准：**

- [ ] 新环境按照 README 使用一条 Docker Compose 命令启动成功，并通过健康检查。
- [ ] GitHub Actions 的必需检查全部通过。
- [ ] 在线 Demo 可打开 5 条预置案例并查看完整的预生成调查轨迹，其中包含 2 条 `supported`、1 条 `insufficient_evidence`、1 条 `budget_exhausted` 和 1 条 `unsafe_request`，且至少 1 条 `supported` 案例展示补丁验证。
- [ ] 90 秒演示视频可访问，并覆盖问题、调查过程、证据和评测结果。
- [ ] 仓库包含架构图、状态机图、数据说明、评测报告、安全模型和失败复盘。
- [ ] 5 位测试者均完成固定任务，反馈以匿名汇总形式记录。
- [ ] 简历材料不使用“自动修复”表述；若补丁成功率不足 50%，明确表述为调查系统与补丁验证探索。
- [ ] API 开发与正式评测的累计费用不超过人民币 300 元。

## 7. 成功指标

### 预注册核心指标

- 主评测集上的修复文件 `Recall@10 ≥ 70%`。
- 调查报告引用有效率 `≥ 90%`。
- 根因判定为 `2` 的比例 `≥ 65%`。
- 10 条可复现案例中，至少 5 条候选补丁通过目标测试且未引入相关回归。
- 完整 Agent 相比“仅向量检索”在主要指标上有明确提升；不隐藏无提升或退化的指标。

### 稳定性、成本与安全指标

- 10 条稳定性子集各运行 3 次，并报告终态一致率、根因结论一致率、成本和延迟波动。
- 单案例工具调用不超过 20 次，检索改写不超过 2 次，补丁尝试不超过 2 次，执行时间不超过 5 分钟。
- 正式报告必须包含单案例及聚合 Token、费用和延迟。
- 安全终止集报告正确终态率、错误根因断言率和越权工具调用次数。
- 越权 GitHub 写操作、白名单外命令、密钥访问和工作目录外路径访问成功次数必须为 0。

### 工程与求职指标

- Docker Compose 一键启动、核心自动化检查和小型检索回归全部通过。
- Git 中保存冻结数据、配置和原始实验结果，关闭 Langfuse 后仍可完成离线复现。
- 在线 Demo、90 秒视频、完整文档、失败复盘和面试材料全部交付。
- 完成 5 位同学的固定任务测试并记录匿名反馈。

## 8. 六周时间盒与降级规则

- **第 1 周（2026-09-15 至 2026-09-21）：** 完成 Phase 1，得到 10 条无泄漏人工案例。
- **第 2 周（2026-09-22 至 2026-09-28）：** 完成 Phase 2，形成三组检索基线和端到端 MVP。
- **第 3 周（2026-09-29 至 2026-10-05）：** 完成 Phase 3，形成可追踪调查报告。
- **第 4 周（2026-10-06 至 2026-10-12）：** 完成 Phase 4，冻结评测并运行基线、消融和安全测试。
- **第 5 周（2026-10-13 至 2026-10-19）：** 完成 Phase 5，加入受限补丁验证和最小界面。
- **第 6 周（2026-10-20 至 2026-10-27）：** 完成 Phase 6，部署、文档、视频、反馈和求职材料。

每周必须产生可运行、可人工验证的增量。若进度不足，依次削减：真实上游贡献、UI 美化、补丁闭环范围、在线实时执行；不得削减时间切片、防泄漏、检索基线、冻结评测、证据引用和安全终止。

若第 3 周检查点 `Recall@10 < 60%`，停止补丁闭环开发，优先修复数据质量、切分、查询构造和检索融合。若最终补丁成功率低于 50%，简历必须将其描述为“证据驱动 Issue 调查系统，并探索受限补丁验证”，不得声称实现自动修复。

## 9. 开发质量要求

- 核心模块必须具备单元测试；跨数据、检索、Agent 和沙箱边界的外部行为使用集成测试验证。
- 测试优先验证公开行为和不变量，不与 LangGraph 内部节点实现细节强耦合。
- 项目作者必须能够解释每个核心模块的输入、输出、关键不变量、替代方案和取舍。
- AI 生成但项目作者无法解释的代码不计入完成成果。
- 每周末完成一次 20 分钟模拟面试录音，覆盖当周新增设计与失败实验。
- 保存重要设计决策、实验配置、失败结果和后续改进，不只保留最终成功版本。
