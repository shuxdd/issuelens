## Agent skills

### 问题跟踪器

本仓库使用 `.scratch/<功能标识>/` 下的本地 Markdown 文件跟踪问题。详见 `docs/agents/issue-tracker.md`。

### 分诊标签

本仓库使用五种默认分诊标签。详见 `docs/agents/triage-labels.md`。

### 领域文档

本仓库采用单上下文领域文档布局。详见 `docs/agents/domain.md`。

### 工单提交

处理 `.scratch/<功能标识>/issues/` 中的工单时，每张工单对应一个独立 Git 提交。当前工单的全部验收检查通过后，在工单的 `## 答案` 中记录结果并将 `Status:` 更新为 `resolved`；然后只暂存该工单相关改动并立即创建提交。验收失败或受外部条件阻塞时保持 `claimed`，不创建完成提交。仅创建本地提交；只有用户明确要求时才推送。

### 工单执行

一次只处理一张明确指定且未受阻的工单。开始实现前读取该工单、总规格、领域文档及相关 ADR，并将状态更新为 `claimed`。实现范围以当前工单的验收项为边界；后续工单所需能力只记录，不提前实现。

用户要求 TDD 时，只在已确认的公共接缝上逐项执行红—绿循环：一次添加一个失败测试，再编写使其通过的最小实现。完成当前工单后运行该工单要求的全部验收检查。

### 跨设备接续

在新电脑接管项目、恢复中断的工单或需要确认后续开发顺序时，先完整阅读 `docs/agents/development-handoff.md`，核对其中的接续检查清单与仓库当前状态后再操作。
